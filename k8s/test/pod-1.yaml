apiVersion: v1
kind: Pod
metadata:
  name: demo-pod-1
  namespace: test2
  labels:
    app: myapp
    env: dev
spec:
  nodeName: node1
  nodeSelector:
    disk: ceph
  containers:
  - name: nginx-pod
    ports:
    - containerPort: 8080
    image: nginx:latest
    imagePullPolicy: IfNotPresent
